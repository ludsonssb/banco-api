spring:
  datasource:
    url: jdbc:mysql://localhost/banco_api?createDatabaseIfNotExist=true&&useSSL=false&&serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
